use ggez::*;
use super::entity_manager::*;
use super::body::*;


pub struct Player {
    body: Body
}

impl Player {
    pub fn update(&mut self) -> () {

    }

    pub fn draw(&self, ctx: &mut Context) -> () {

    }
}
